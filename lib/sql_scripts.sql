select corpora.name, corpora.id,documents.id,published_time from documents INNER JOIN corpora ON documents.corpus_id = corpora.id Where corpora.id=13 and date(documents.published_time) > 0 order by documents.published_time limit 1;
select corpora.name, corpora.id,documents.id,published_time from documents INNER JOIN corpora ON documents.corpus_id = corpora.id Where corpora.id=13 and date(documents.published_time) > 0 order by documents.published_time desc limit 1;
select corpora.name, corpora.id,documents.id,published_time from documents INNER JOIN corpora ON documents.corpus_id = corpora.id Where corpora.id=14 and date(documents.published_time) > 0 order by documents.published_time limit 1;
select corpora.name, corpora.id,documents.id,published_time from documents INNER JOIN corpora ON documents.corpus_id = corpora.id Where corpora.id=14 and date(documents.published_time) > 0 order by documents.published_time desc limit 1;
select corpora.name, corpora.id,documents.id,published_time from documents INNER JOIN corpora ON documents.corpus_id = corpora.id Where corpora.id=15 and date(documents.published_time) > 0 order by documents.published_time limit 1;
select corpora.name, corpora.id,documents.id,published_time from documents INNER JOIN corpora ON documents.corpus_id = corpora.id Where corpora.id=15 and date(documents.published_time) > 0 order by documents.published_time desc limit 1;
select corpora.name, corpora.id,documents.id,published_time from documents INNER JOIN corpora ON documents.corpus_id = corpora.id Where corpora.id=16 and date(documents.published_time) > 0 order by documents.published_time limit 1;
select corpora.name, corpora.id,documents.id,published_time from documents INNER JOIN corpora ON documents.corpus_id = corpora.id Where corpora.id=16 and date(documents.published_time) > 0 order by documents.published_time desc limit 1;
select corpora.name, corpora.id,documents.id,published_time from documents INNER JOIN corpora ON documents.corpus_id = corpora.id Where corpora.id=17 and date(documents.published_time) > 0 order by documents.published_time limit 1;
select corpora.name, corpora.id,documents.id,published_time from documents INNER JOIN corpora ON documents.corpus_id = corpora.id Where corpora.id=17 and date(documents.published_time) > 0 order by documents.published_time desc limit 1;
select corpora.name, corpora.id,documents.id,published_time from documents INNER JOIN corpora ON documents.corpus_id = corpora.id Where corpora.id=18 and date(documents.published_time) > 0 order by documents.published_time limit 1;
select corpora.name, corpora.id,documents.id,published_time from documents INNER JOIN corpora ON documents.corpus_id = corpora.id Where corpora.id=18 and date(documents.published_time) > 0 order by documents.published_time desc limit 1;
select corpora.name, corpora.id,documents.id,published_time from documents INNER JOIN corpora ON documents.corpus_id = corpora.id Where corpora.id=19 and date(documents.published_time) > 0 order by documents.published_time limit 1;
select corpora.name, corpora.id,documents.id,published_time from documents INNER JOIN corpora ON documents.corpus_id = corpora.id Where corpora.id=19 and date(documents.published_time) > 0 order by documents.published_time desc limit 1;
